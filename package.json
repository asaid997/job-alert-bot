{
  "name": "job-alert-bot",
  "private": true,
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/python -m pip install playwright && .venv/bin/python -m playwright install chromium",
    "ensure": "test -x .venv/bin/python || npm run setup",
    "open-linkedin": "npm run ensure && .venv/bin/python linkedin_open.py",
    "script.py": "npm run open-linkedin"
  }
}
